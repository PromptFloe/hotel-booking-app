import React from 'react'; import BookingCard from '../components/bookings/BookingCard'; import BookingForm from '../components/bookings/BookingForm'; import BookingFilters from '../components/bookings/BookingFilters'; import { useBookings } from '../context/BookingContext'; const Bookings: React.FC = () => { const { bookings } = useBookings(); return (<div className="space-y-6"><h1 className="text-4xl font-bold text-textPrimary">Bookings</h1><BookingFilters /><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{bookings.map(booking => (<BookingCard key={booking.id} booking={booking} />))}</div><BookingForm /></div>); }; export default Bookings;