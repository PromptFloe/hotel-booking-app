import React from 'react'; import TaskCard from '../components/housekeeping/TaskCard'; import TaskForm from '../components/housekeeping/TaskForm'; import { housekeepingTasks } from '../data/housekeeping'; const Housekeeping: React.FC = () => { return (<div className="space-y-6"><h1 className="text-4xl font-bold text-textPrimary">Housekeeping</h1><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{housekeepingTasks.map(task => (<TaskCard key={task.id} task={task} />))}</div><TaskForm /></div>); }; export default Housekeeping;