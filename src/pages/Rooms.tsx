import React from 'react'; import RoomCard from '../components/rooms/RoomCard'; import RoomForm from '../components/rooms/RoomForm'; import RoomFilters from '../components/rooms/RoomFilters'; import { useRooms } from '../context/RoomContext'; const Rooms: React.FC = () => { const { rooms } = useRooms(); return (<div className="space-y-6"><h1 className="text-4xl font-bold text-textPrimary">Rooms</h1><RoomFilters /><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{rooms.map(room => (<RoomCard key={room.id} room={room} />))}</div><RoomForm /></div>); }; export default Rooms;