import React from 'react'; import GuestCard from '../components/guests/GuestCard'; import GuestForm from '../components/guests/GuestForm'; import { guests } from '../data/guests'; const Guests: React.FC = () => { return (<div className="space-y-6"><h1 className="text-4xl font-bold text-textPrimary">Guests</h1><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{guests.map(guest => (<GuestCard key={guest.id} guest={guest} />))}</div><GuestForm /></div>); }; export default Guests;