import React, { useState } from 'react'; import { Button } from '../ui/Button'; import { Input } from '../ui/Input'; import { Select } from '../ui/Select'; import { useRooms } from '../../context/RoomContext'; import { Room } from '../../types'; const RoomForm: React.FC = () => { const { rooms, updateRoomStatus } = useRooms(); const [formData, setFormData] = useState<Partial<Room>>({ number: '', type: 'single', status: 'available', price: 0 }); const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => { setFormData({ ...formData, [e.target.name]: e.target.value }); }; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (formData.number && formData.price) { updateRoomStatus(formData.number, formData.status as Room['status']); } }; return (<form onSubmit={handleSubmit} className="space-y-4"><Input name="number" placeholder="Room Number" value={formData.number} onChange={handleChange} required /><Select name="type" value={formData.type} onChange={handleChange}><option value="single">Single</option><option value="double">Double</option><option value="suite">Suite</option></Select><Input name="price" type="number" placeholder="Price" value={formData.price} onChange={handleChange} required /><Button type="submit">Update Room</Button></form>); }; export default RoomForm;