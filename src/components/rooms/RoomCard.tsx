import React from 'react'; import { Room } from '../../types'; import { Card } from '../ui/Card'; import { Badge } from '../ui/Badge'; interface RoomCardProps { room: Room; } const RoomCard: React.FC<RoomCardProps> = ({ room }) => { return (<Card><div className="flex justify-between items-center"><div><div className="text-lg font-semibold text-textPrimary">Room {room.number}</div><div className="text-sm text-textSecondary">Type: {room.type}</div><div className="text-sm text-textSecondary">Price: ₹{room.price}</div></div><Badge status={room.status === 'available' ? 'success' : room.status === 'occupied' ? 'warning' : 'danger'}>{room.status}</Badge></div></Card>); }; export default RoomCard;