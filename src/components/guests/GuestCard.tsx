import React from 'react'; import { Guest } from '../../types'; import { Card } from '../ui/Card'; import { Avatar } from '../ui/Avatar'; interface GuestCardProps { guest: Guest; } const GuestCard: React.FC<GuestCardProps> = ({ guest }) => { return (<Card><div className="flex items-center gap-4"><Avatar src={guest.avatarUrl} alt={guest.name} /><div><div className="text-lg font-semibold text-textPrimary">{guest.name}</div><div className="text-sm text-textSecondary">Email: {guest.email}</div><div className="text-sm text-textSecondary">Phone: {guest.phone}</div></div></div></Card>); }; export default GuestCard;