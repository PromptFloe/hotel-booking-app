import React from 'react'; interface BadgeProps { status: 'success' | 'warning' | 'danger' | 'info' | 'neutral'; children: React.ReactNode; } const Badge: React.FC<BadgeProps> = ({ status, children }) => { const statusClasses = { success: 'bg-success text-white', warning: 'bg-warning text-white', danger: 'bg-danger text-white', info: 'bg-primary text-white', neutral: 'bg-surface2 text-textSecondary' }; return (<span className={`px-2.5 py-0.5 rounded-full text-xs font-medium ${statusClasses[status]}`}>{children}</span>); }; export default Badge;