import React from 'react'; interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> { variant?: 'primary' | 'secondary'; } const Button: React.FC<ButtonProps> = ({ variant = 'primary', children, ...props }) => { const baseClasses = 'px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2'; const variantClasses = variant === 'primary' ? 'bg-primary text-white hover:bg-primaryHover focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2' : 'bg-surface2 text-textSecondary hover:bg-surface3'; return (<button className={`${baseClasses} ${variantClasses}`} {...props}>{children}</button>); }; export default Button;