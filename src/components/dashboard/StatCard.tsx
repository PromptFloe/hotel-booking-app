import React from 'react'; import { Badge } from '../ui/Badge'; interface StatCardProps { title: string; value: string; trend: 'up' | 'down'; trendValue: string; } const StatCard: React.FC<StatCardProps> = ({ title, value, trend, trendValue }) => { const trendColor = trend === 'up' ? 'text-success' : 'text-danger'; return (<div className="rounded-xl border bg-white shadow-sm p-6"><div className="text-sm text-textSecondary">{title}</div><div className="text-3xl font-bold text-textPrimary mt-2">{value}</div><div className={`inline-flex items-center gap-1 text-xs font-medium ${trendColor}`}><Badge status={trend === 'up' ? 'success' : 'danger'}>{trend === 'up' ? '↑' : '↓'} {trendValue}</Badge></div></div>); }; export default StatCard;