import React from 'react'; import { HousekeepingTask } from '../../types'; import { Card } from '../ui/Card'; import { Badge } from '../ui/Badge'; interface TaskCardProps { task: HousekeepingTask; } const TaskCard: React.FC<TaskCardProps> = ({ task }) => { return (<Card><div className="flex justify-between items-center"><div><div className="text-lg font-semibold text-textPrimary">Task #{task.id}</div><div className="text-sm text-textSecondary">Room: {task.roomId}</div><div className="text-sm text-textSecondary">{task.description}</div></div><Badge status={task.status === 'completed' ? 'success' : 'warning'}>{task.status}</Badge></div></Card>); }; export default TaskCard;