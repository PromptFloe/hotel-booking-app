import React, { useState } from 'react'; import { Button } from '../ui/Button'; import { Input } from '../ui/Input'; import { HousekeepingTask } from '../../types'; const TaskForm: React.FC = () => { const [formData, setFormData] = useState<Partial<HousekeepingTask>>({ roomId: '', description: '', status: 'pending' }); const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => { setFormData({ ...formData, [e.target.name]: e.target.value }); }; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (formData.roomId && formData.description) { console.log('Task added:', formData); } }; return (<form onSubmit={handleSubmit} className="space-y-4"><Input name="roomId" placeholder="Room ID" value={formData.roomId} onChange={handleChange} required /><Input name="description" placeholder="Description" value={formData.description} onChange={handleChange} required /><Button type="submit">Add Task</Button></form>); }; export default TaskForm;