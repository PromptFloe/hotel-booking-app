import React, { useState } from 'react'; import { Button } from '../ui/Button'; import { Input } from '../ui/Input'; import { Select } from '../ui/Select'; import { useBookings } from '../../context/BookingContext'; import { Booking } from '../../types'; const BookingForm: React.FC = () => { const { addBooking } = useBookings(); const [formData, setFormData] = useState<Partial<Booking>>({ guestId: '', roomId: '', checkIn: new Date(), checkOut: new Date(), status: 'pending' }); const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => { setFormData({ ...formData, [e.target.name]: e.target.value }); }; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (formData.guestId && formData.roomId) { addBooking({ ...formData, id: Date.now().toString() } as Booking); } }; return (<form onSubmit={handleSubmit} className="space-y-4"><Input name="guestId" placeholder="Guest ID" value={formData.guestId} onChange={handleChange} required /><Input name="roomId" placeholder="Room ID" value={formData.roomId} onChange={handleChange} required /><Select name="status" value={formData.status} onChange={handleChange}><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="cancelled">Cancelled</option></Select><Button type="submit">Add Booking</Button></form>); }; export default BookingForm;