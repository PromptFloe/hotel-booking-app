import React from 'react'; import { Booking } from '../../types'; import { Card } from '../ui/Card'; import { Badge } from '../ui/Badge'; interface BookingCardProps { booking: Booking; } const BookingCard: React.FC<BookingCardProps> = ({ booking }) => { return (<Card><div className="flex justify-between items-center"><div><div className="text-lg font-semibold text-textPrimary">Booking #{booking.id}</div><div className="text-sm text-textSecondary">Room: {booking.roomId}</div><div className="text-sm text-textSecondary">Guest: {booking.guestId}</div></div><Badge status={booking.status === 'confirmed' ? 'success' : booking.status === 'pending' ? 'warning' : 'danger'}>{booking.status}</Badge></div></Card>); }; export default BookingCard;