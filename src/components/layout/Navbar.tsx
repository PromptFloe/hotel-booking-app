import React from 'react'; import { useAuth } from '../../context/AuthContext'; import { LogOut } from 'lucide-react'; const Navbar: React.FC = () => { const { user, logout } = useAuth(); return (<header className="h-16 bg-surface1 border-b border-border flex items-center justify-between px-6"><div className="text-lg font-semibold text-textPrimary">Welcome to HotelSys</div><div className="flex items-center gap-4"><span className="text-textSecondary">{user?.name}</span><button onClick={logout} className="flex items-center gap-2 text-textSecondary hover:text-textPrimary"><LogOut className="w-5 h-5" />Logout</button></div></header>); }; export default Navbar;