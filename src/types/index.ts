export interface User { id: string; name: string; email: string; role: 'admin' | 'staff'; avatarUrl: string; } export interface Booking { id: string; guestId: string; roomId: string; checkIn: Date; checkOut: Date; status: 'confirmed' | 'pending' | 'cancelled'; } export interface Room { id: string; number: string; type: 'single' | 'double' | 'suite'; status: 'available' | 'occupied' | 'maintenance'; price: number; } export interface Guest { id: string; name: string; email: string; phone: string; avatarUrl: string; } export interface HousekeepingTask { id: string; roomId: string; description: string; status: 'pending' | 'completed'; }